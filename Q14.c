#include <stdio.h>
int main () {
    int n;
    printf ("enter a alphabet : ");
    scanf  ("%d",&n);
    int a , e , i, o, u;
    if (n== a,e,i,o,u){
        printf (" alphabet is a vowel ");
    }
    else 
    { printf ("alphabet is not a vowel"); }
    return 0;
}